services:
  habilitfy-app:
    build:
      context: ../..
      dockerfile: factory/docker/Dockerfile.app
    env_file:
      - ../../.env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 5000
    ports:
      - "5000:5000"
    restart: unless-stopped
